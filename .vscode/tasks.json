{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug-build-win",
            "type": "shell",
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug -S . -B build ; cd build ; cmake --build .",
        }
    ]
}